<UserControl x:Class="simulation_app.Views.SimulatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:m="clr-namespace:simulation_app.Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <UserControl.Resources>

        <!-- ValveType 전체 값(ComboBox 바인딩용) -->
        <ObjectDataProvider x:Key="ValveTypes" MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="m:ValveType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <!-- DataGrid 공통 스타일(가벼운 미관) -->
        <Style TargetType="DataGrid">
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- ===== 좌측: 설정/목록/조작 ===== -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Tank 설정" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>

                <!-- Capacity -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="Capacity (L)" Width="120" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Tank.Capacity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Width="120"/>
                </StackPanel>

                <Separator Margin="0,6,0,6"/>

                <!-- Valves -->
                <TextBlock Text="Valves" FontWeight="Bold" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <Button Content="Add In"  Width="80" Margin="0,0,6,0"
                            Command="{Binding AddInValveCmd}"/>
                    <Button Content="Add Out" Width="80" Margin="0,0,6,0"
                            Command="{Binding AddOutValveCmd}"/>
                    <Button Content="Remove"  Width="80"
                            Command="{Binding RemoveSelectedValveCmd}"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding Valves}"
                          SelectedItem="{Binding SelectedValve, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name"
                                            Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*"/>
                        <!-- Type: ComboBox (Enum) -->
                        <DataGridTemplateColumn Header="Type" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Type}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding Source={StaticResource ValveTypes}}"
                                              SelectedItem="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              MinWidth="90"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Flow (L/s)"
                                            Binding="{Binding FlowRate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="110"/>
                        <!-- Open: 즉시 커밋 -->
                        <DataGridTemplateColumn Header="Open" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsOpen, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsOpen, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Sensors -->
                <TextBlock Text="Level Sensors" FontWeight="Bold" Margin="0,8,0,6"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <Button Content="Add Sensor" Width="100" Margin="0,0,6,0"
                            Command="{Binding AddSensorCmd}"/>
                    <Button Content="Remove" Width="80"
                            Command="{Binding RemoveSelectedSensorCmd}"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding Sensors}"
                          SelectedItem="{Binding SelectedSensor, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name"
                                            Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*"/>
                        <DataGridTextColumn Header="Trigger (L)"
                                            Binding="{Binding TriggerLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="120"/>
                        <DataGridCheckBoxColumn Header="On"
                                                Binding="{Binding IsOn}"
                                                IsReadOnly="True"
                                                Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Separator Margin="0,6,0,6"/>

                <!-- Commands -->
                <StackPanel Orientation="Horizontal">
                    <Button Content="Start" Width="80" Margin="0,0,6,0"
                            Command="{Binding StartCmd}"/>
                    <Button Content="Stop"  Width="80" Margin="0,0,6,0"
                            Command="{Binding StopCmd}"/>
                    <Button Content="Reset" Width="80"
                            Command="{Binding ResetCmd}"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- ===== 우측: 탱크 시각화 ===== -->
        <Grid Grid.Column="1">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Width="260" Height="360" BorderThickness="3" BorderBrush="Gray"
                        CornerRadius="6" Background="#11CCCCCC">
                    <Grid>
                        <!-- 수위(아래서부터 차오름) -->
                        <Rectangle Fill="#3399CC"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Bottom"
                                   Height="{Binding WaterHeight}"/>
                    </Grid>
                </Border>

                <!-- 정보 라벨 -->
                <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="280,0,0,0">
                    <TextBlock Text="Top"/>
                    <TextBlock Text="{Binding Tank.Capacity, StringFormat=Cap: {0} L}"/>
                    <TextBlock Text="{Binding Tank.Level,    StringFormat=Level: {0:0.##} L}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
